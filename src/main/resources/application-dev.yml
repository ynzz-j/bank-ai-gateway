# 开发环境配置（使用已有数据库）
server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_ai_gateway
    username: bank_gateway
    password: Bank@Gateway2026

  data:
    redis:
      host: localhost
      port: 6379
      password: redis123

jwt:
  secret: dev-secret-key-for-development-only-must-be-at-least-256-bits-long-for-hs256

kms:
  type: local
  local:
    key: dev-kms-key-32-bytes-long-abcdef01

logging:
  level:
    root: INFO
    com.bank.ai.gateway: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,beans,mappings