<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bank.ai.gateway.repository.AuditLogMapper">

    <resultMap id="AuditLogResultMap" type="com.bank.ai.gateway.model.entity.AuditLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="action" property="action"/>
        <result column="resource_type" property="resourceType"/>
        <result column="resource_id" property="resourceId"/>
        <result column="resource_name" property="resourceName"/>
        <result column="change_snapshot" property="changeSnapshot"/>
        <result column="request_ip" property="requestIp"/>
        <result column="user_agent" property="userAgent"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="baseColumns">
        id, user_id, username, action, resource_type, resource_id, resource_name,
        change_snapshot, request_ip, user_agent, created_at
    </sql>

    <insert id="insert" parameterType="com.bank.ai.gateway.model.entity.AuditLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_log (
            user_id, username, action, resource_type, resource_id, resource_name,
            change_snapshot, request_ip, user_agent, created_at
        ) VALUES (
            #{userId}, #{username}, #{action}, #{resourceType}, #{resourceId}, #{resourceName},
            #{changeSnapshot}, #{requestIp}, #{userAgent}, #{createdAt}
        )
    </insert>

    <select id="findById" resultMap="AuditLogResultMap">
        SELECT <include refid="baseColumns"/>
        FROM audit_log
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="AuditLogResultMap">
        SELECT <include refid="baseColumns"/>
        FROM audit_log
        WHERE user_id = #{userId}
          AND created_at BETWEEN #{startTime} AND #{endTime}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectByResource" resultMap="AuditLogResultMap">
        SELECT <include refid="baseColumns"/>
        FROM audit_log
        WHERE resource_type = #{resourceType}
          AND resource_id = #{resourceId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="selectPage" resultMap="AuditLogResultMap">
        SELECT <include refid="baseColumns"/>
        FROM audit_log
        <where>
            <if test="action != null and action != ''">AND action = #{action}</if>
            <if test="resourceType != null and resourceType != ''">AND resource_type = #{resourceType}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="startTime != null">AND created_at &gt;= #{startTime}</if>
            <if test="endTime != null">AND created_at &lt;= #{endTime}</if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countLogs" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM audit_log
        <where>
            <if test="action != null and action != ''">AND action = #{action}</if>
            <if test="resourceType != null and resourceType != ''">AND resource_type = #{resourceType}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="startTime != null">AND created_at &gt;= #{startTime}</if>
            <if test="endTime != null">AND created_at &lt;= #{endTime}</if>
        </where>
    </select>

    <select id="statsByAction" resultType="java.util.Map">
        SELECT
            action,
            COUNT(*) as count
        FROM audit_log
        WHERE created_at BETWEEN #{startTime} AND #{endTime}
        GROUP BY action
        ORDER BY count DESC
    </select>

    <select id="statsByResource" resultType="java.util.Map">
        SELECT
            resource_type,
            COUNT(*) as count
        FROM audit_log
        WHERE created_at BETWEEN #{startTime} AND #{endTime}
        GROUP BY resource_type
        ORDER BY count DESC
    </select>

    <select id="statsByUser" resultType="java.util.Map">
        SELECT
            user_id,
            username,
            COUNT(*) as count
        FROM audit_log
        WHERE created_at BETWEEN #{startTime} AND #{endTime}
        GROUP BY user_id, username
        ORDER BY count DESC
    </select>

    <select id="selectByActions" resultMap="AuditLogResultMap">
        SELECT <include refid="baseColumns"/>
        FROM audit_log
        WHERE action IN
        <foreach collection="actions" item="action" open="(" separator="," close=")">
            #{action}
        </foreach>
        AND created_at BETWEEN #{startTime} AND #{endTime}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByActions" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM audit_log
        WHERE action IN
        <foreach collection="actions" item="action" open="(" separator="," close=")">
            #{action}
        </foreach>
        AND created_at BETWEEN #{startTime} AND #{endTime}
    </select>

</mapper>