<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bank.ai.gateway.repository.compliance.ViolationLogMapper">

    <resultMap id="BaseResultMap" type="com.bank.ai.gateway.model.entity.ViolationLog">
        <id column="id" property="id"/>
        <result column="api_key_id" property="apiKeyId"/>
        <result column="user_id" property="userId"/>
        <result column="call_log_id" property="callLogId"/>
        <result column="filter_name" property="filterName"/>
        <result column="matched_word" property="matchedWord"/>
        <result column="category" property="category"/>
        <result column="action" property="action"/>
        <result column="content_snippet" property="contentSnippet"/>
        <result column="direction" property="direction"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="BaseColumns">
        id, api_key_id, user_id, call_log_id, filter_name, matched_word, category, action, content_snippet, direction, created_at
    </sql>

    <insert id="insert" parameterType="com.bank.ai.gateway.model.entity.ViolationLog">
        INSERT INTO violation_logs (api_key_id, user_id, call_log_id, filter_name, matched_word, category, action, content_snippet, direction, created_at)
        VALUES (#{apiKeyId}, #{userId}, #{callLogId}, #{filterName}, #{matchedWord}, #{category}, #{action}, #{contentSnippet}, #{direction}, #{createdAt})
    </insert>

    <select id="selectByApiKeyId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM violation_logs
        WHERE api_key_id = #{apiKeyId}
        <if test="startTime != null">
            AND created_at &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND created_at &lt;= #{endTime}
        </if>
        ORDER BY created_at DESC
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

    <select id="countByApiKeyId" resultType="int">
        SELECT COUNT(*)
        FROM violation_logs
        WHERE api_key_id = #{apiKeyId}
        <if test="startTime != null">
            AND created_at &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            AND created_at &lt;= #{endTime}
        </if>
    </select>

</mapper>
